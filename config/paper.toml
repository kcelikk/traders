# fbot paper trading — Faz 7. Kayıt ayarları recorder.toml ile aynı şemadır; ek olarak çekirdek/karar/risk parametreleri.
# Kârlılık gösterilmedi (ADR 0010): [decision].allowed_cells boş olduğu sürece hiçbir giriş emri üretilmez.

[run]
out_dir = "data/recordings"
rotate_minutes = 60
snapshot_interval_s = 600
depth_snapshot_limit = 1000
stats_interval_s = 10
tick_ms = 1000

[universe]
mode = "top"
top_n = 10
exclude_bases = ["USDC", "FDUSD", "TUSD", "BUSD", "USD1", "USDE", "USDP", "DAI"]

[streams]
profile = "trader_paper"       # depth gerekli: dolum simülatörünün girdisi (ADR 0013)
public = ["{s}@bookTicker", "{s}@depth@100ms"]
market = ["{s}@aggTrade", "{s}@markPrice@1s", "{s}@forceOrder"]

[staleness_s]
public = 30
market = 30

[reconnect]
backoff_initial_s = 1.0
backoff_max_s = 30.0

[core]
bar_ms = 60000

[state_engine]                 # Faz 3 ile aynı; ısınma 2W bar (W=240 → 8 saat)
W = 240
N_short = 5
N_long = 15
p_lo = 0.2
p_hi = 0.8

[decision]
notional_usdt = "80"           # ADR 0004
max_state_age_bars = 3
spread_mult = "2"
funding_guard_ms = 120000
report_hash = "a693aa0742076410"
allowed_cells = []             # Faz 3'te maliyet üstü beklenti gösteren hücre yok → giriş yok
# örnek: allowed_cells = [{ state = "S1", dir = "long", h = 15 }]
sl_pct = {}                    # ölçülene kadar boş: sl/tp yoksa intent üretilmez (fail-closed)
tp_pct = {}
research_spread_bps = { BNBUSDT = "0.14", BTCUSDT = "0.013", DOGEUSDT = "1.192", ETHUSDT = "0.041", HYPEUSDT = "0.125", IOSTUSDT = "2.031", NEARUSDT = "0.5", SOLUSDT = "1.004", VTHOUSDT = "3.385", XRPUSDT = "0.742", ZECUSDT = "0.091" }   # ölçüldü: rec-72h son 3 saat, bookTicker medyanı (NEAR evrende değildi, tahmin)

[position]                     # ADR 0012
t_protect_ms = 3000
t_backup_ms = 1500
working_type = "MARK_PRICE"
price_protect = true
min_replace_interval_ms = 5000
taker_fee_pct = "0.05"         # VIP0, doğrulanmadı (docs/unit-economics.md)
maker_fee_pct = "0.02"
max_hold_ms = 3600000
lock_trigger_pct = "0.30"
lock_offset_pct = "0.05"
trail_step_pct = "0.10"
trail_gap_pct = "0.30"
degrade_map = { S1 = ["S2"], S2 = ["S1"] }
# tp1_pct / tp1_frac: ölçülmedi → kapalı

[risk]
max_positions = 5
gross_cap_usdt = "400"
default_leverage = 5
leverage = { BTCUSDT = 10, ETHUSDT = 10 }
margin_buffer = "0.2"
spread_max_bps = "5"
reserve_orders = 3
backoff_ms = 10000
warmup_bars = 480              # 2W
beta_cap_usdt = "750"          # ADR 0014: 30 günlük beta ölçümü (docs/research/beta-dagilimi.md)
# cooldown_*, participation_max, slippage_max_bps: ölçülmedi → kapalı

[sim]                          # dolum simülatörü — ADR 0013 (NautilusTrader/Hummingbot kuralları, L2 defter yürüyüşü)
latency_ms = 400               # Faz 0 ölçümü: p50 ≈ 420 ms
jitter_ms = 600                # p99 ≈ 1 s → [400, 1000] ms, seed'li
seed = 20260910
partial_timeout_ms = 5000      # kısmi dolumda kalan miktar bu süre sonunda iptal
prob_fill_on_touch = 0.0       # maker: dokunma yetmez, geçiş gerekir (muhafazakâr)
book_levels = 20               # simülatöre verilen L2 derinlik

[account]                      # paper: sanal hesap; borsa yok → kaldıraç görünümü config'in kendisi
paper = true
available_balance = "1000"

[cost_drift]                   # BÖLÜM 6.4 — alarm üretir, işlem ENGELLEMEZ
window_ms = 86400000           # 24 saat
capital_usdt = "1000"
commission_to_gross_max = "0.30"   # komisyon / brüt kâr
cost_to_capital_max = "0.02"       # toplam maliyet / sermaye
net_per_trade_min = "-0.05"        # işlem başına ortalama net (%)
min_trades = 20
