[pytest]
testpaths = tests
addopts = -q
markers =
    slow: uzun süren test (benchmark, gerçek kayıt)
    net: ağ erişimi gerektirir; varsayılan koşuda çalışmaz
    golden: sabitlenmiş baseline karşılaştırması
